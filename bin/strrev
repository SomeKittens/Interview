#!/usr/bin/env node

'use strict';

var toReverse = process.argv
                            .slice(2)
                            .join(' ')
                            .split('')
                            .reverse()
                            .join('');

console.log(toReverse);